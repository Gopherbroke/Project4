<h1>Piece Show</h1>

<%= @piece.name %>
<%= @piece.description %>


<br>
<br>
<%= form_for ([@piece, @piece.comments.build]) do |f| %>

  <article>
    <h3>Leave A Comment</h3>
    <%= f.label :body %>
    <%= f.text_area :body %>
  </article>

  <article>
    <%= f.submit %>
  </article>

  <% end %>
