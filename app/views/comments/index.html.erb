<h1>Comment Index</h1>
<article>

<% @comments.each do |comment| %>
<div>
  <%= comment.body %>

  <%= link_to 'Edit', edit_piece_comment_path(@piece, comment) %>
  <%= link_to 'Destroy Comment', [@piece, comment], method: :delete, data: { confirm: 'Are you sure?'} %>
</div>
<% end %>
</article>
